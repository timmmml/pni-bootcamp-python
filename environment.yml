name: pni-python-bootcamp
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy
  - scipy
  - matplotlib
  - jupyter
  - pip
  - pip:
      - hmmlearn   # optional: only used for the cross-check cell in 03_hmm_project
